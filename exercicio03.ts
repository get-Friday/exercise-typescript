let aluno: {
    nome: string,
    matricula: number,
    ativo: boolean,
    cadeiras: string[] 
}

aluno.nome = 'Gabriel'
aluno.matricula = 123456
aluno.ativo = true
aluno.cadeiras = ['gamer', 'escritório', 'cozinha', 'praça']

console.log(aluno)